<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/main.css">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="container">
        <h2>Breakfast Blogs</h2>

        <br>

        <div class="flex font-sans">
            <!-- Add food image -->
            <div class="flex-none w-48 relative">
                <img src="#" alt="food Image" class="absolute inset-0 w-full h-full object-cover" loading="lazy" />
            </div>

            <form class="flex-auto p-6">
                <div class="flex flex-wrap">
                    <!-- Food name -->
                    <h1 class="flex-auto text-lg font-semibold text-slate-900"> Dosa </h1>

                    <!-- Author -->
                    <div class="text-lg font-bold text-slate-600">
                        Author name
                    </div>

                    <!-- Date published -->
                    <div class="w-full flex-none text-sm font-medium text-slate-700 mt-2">
                        Date
                    </div>
                </div>

                <div class="flex items-baseline mt-4 mb-6 pb-6 border-b border-slate-200">
                    <div class="space-x-2 flex text-sm">
                        A thin, crispy crepe made from fermented rice and lentil batter, typically served with coconut
                        chutney, sambar (a spicy lentil-based stew), and various fillings such as potatoes or paneer.",
                        author: "Spice Delight
                    </div>
                </div>

                <div class="flex space-x-4 mb-6 text-sm font-medium">
                    <div class="flex-auto flex space-x-4">
                        <button class="h-10 px-6 font-semibold rounded-md bg-black text-white" type="submit">
                            Read more
                        </button>
                        <button class="h-10 px-6 font-semibold rounded-md border border-slate-200 text-slate-900"
                            type="button">
                            Share
                        </button>
                    </div>

                    <!-- Like button -->
                    <button
                        class="flex-none flex items-center justify-center w-9 h-9 rounded-md text-slate-300 border border-slate-200"
                        type="button" aria-label="Like" onclick="toggleFill()">
                        <svg id="heartIcon" width="20" height="20" fill="currentColor" aria-hidden="true">
                            <path fill-rule="evenodd" clip-rule="evenodd"
                                d="M3.172 5.172a4 4 0 015.656 0L10 6.343l1.172-1.171a4 4 0 115.656 5.656L10 17.657l-6.828-6.829a4 4 0 010-5.656z" />
                        </svg>
                    </button>

                </div>
                <p class="text-sm text-slate-700">
                    Free shipping on all continental US orders.
                </p>
            </form>
        </div>
    </div>

    <script>
        function toggleFill() {
            var heartIcon = document.getElementById('heartIcon');
            if (heartIcon.getAttribute('fill') === 'red') {
                heartIcon.setAttribute('fill', 'currentColor');
            } else {
                heartIcon.setAttribute('fill', 'red');
            }
        }
    </script>
</body>

</html>